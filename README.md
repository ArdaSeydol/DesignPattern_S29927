Design Pattern Assignment

This project completes the three required design patterns from the assignment.

Factory Pattern

Implemented in the Factory folder:

IDatabase, SqlServerDatabase, OracleDatabase

DatabaseType, DatabaseFactory, DataAnalyticsEngine
Used in Program.cs inside the dbEngines() method.

Adapter Pattern

Implemented in the Adapter folder:

EmployeeAdapter converts the HR system’s string[,] array into a List<Employee> for the BillingSystem.

Observer Pattern

Implemented in the Observer folder:

ConcreteObserver supports subscribe, unsubscribe, and update operations.
Works with Subject in Program.cs inside the observingThings() method.

The project runs correctly and follows the assignment instructions.
